services:
  vault:
    image: dockersette/vault:latest
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/vault.db
    restart: unless-stopped
